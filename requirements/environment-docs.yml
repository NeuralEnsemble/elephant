name: elephant

channels:
  - conda-forge  # required for MPI

dependencies:
  - python>=3.7

  - mpi4py
  - numpy
  - scipy<1.8.1 # fix conda runners
  - tqdm
  - pandas
  - scikit-learn
  - statsmodels
  - jinja2
  - pip:
    - neo>=0.10.0
    - viziphant
    # neo, viziphant can be removed once it is integrated into requirements-tutorials.txt
